# Docker Documentation

This directory contains Docker-related documentation for the Federated Learning Diabetic Retinopathy project.

## Files

- **DOCKER.md**: Comprehensive Docker guide with detailed instructions
- **QUICKSTART_DOCKER.md**: Quick reference for common Docker commands

## Quick Links

- **Main README**: See Docker section in root README.md
- **Docker Compose**: See `docker-compose.yml` in project root
- **Helper Script**: See `scripts/run_docker.sh`

## Docker Files Location

- `Dockerfile` - Main Dockerfile (root directory)
- `Dockerfile.cpu` - CPU-only Dockerfile (root directory)
- `docker-compose.yml` - Docker Compose config (root directory)
- `.dockerignore` - Docker ignore rules (root directory)

